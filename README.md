# Python OpenAI Chat Application

A Python script that uses the OpenAI API to process user prompts and provide AI-generated responses.

## Features

- Accepts user prompts.
- Processes prompts using OpenAI's GPT-3.5-turbo model.

## Requirements

- Python 3.7 or higher
- OpenAI API key (stored in a `.env` file)
